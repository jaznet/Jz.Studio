import { JzButtonBlockPart } from './jz-button-block-part';

describe('JzButtonBlockPart', () => {
  it('should create an instance', () => {
    expect(new JzButtonBlockPart()).toBeTruthy();
  });
});
