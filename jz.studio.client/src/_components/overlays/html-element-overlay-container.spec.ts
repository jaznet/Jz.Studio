import { HtmlElementOverlayContainer } from './html-element-overlay-container';

describe('HtmlElementOverlayContainer', () => {
  it('should create an instance', () => {
    expect(new HtmlElementOverlayContainer()).toBeTruthy();
  });
});
