import { PaintTestPatternStrategy } from './paint-test-pattern';

describe('PaintStrategyTestPattern', () => {
  it('should create an instance', () => {
  //  expect(new PaintTestPatternStrategy()).toBeTruthy();
  });
});
