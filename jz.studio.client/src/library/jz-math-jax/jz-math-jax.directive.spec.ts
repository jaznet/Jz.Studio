import { JzMathJaxDirective } from './jz-math-jax.directive';

describe('JzMathJaxDirective', () => {
  it('should create an instance', () => {
    //const directive = new JzMathJaxDirective(any);
    //expect(directive).toBeTruthy();
  });
});
