diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/macd/macd-chart.component.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/macd/macd-chart.component.ts
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/macd/macd-chart.component.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/macd/macd-chart.component.ts
@@ -123,7 +123,7 @@
       .data([0])
       .join('line')
       .attr('class', 'macd-baseline')
-      .attr('x1', 0).attr('x2', panel.panelRect.width)
+      .attr('x1', 0).attr('x2', panel.innerWidth)
       .attr('y1', y(0)).attr('y2', y(0));
 
     // histogram bars
@@ -132,11 +132,12 @@
       .data(dates.map((dt, i) => ({ dt, v: hist[i] })))
       .join('rect')
       .attr('class', 'macd-hist')
+      .classed('macd-hist--positive', d => (d.v ?? 0) >= 0)
+      .classed('macd-hist--negative', d => (d.v ?? 0) < 0)
       .attr('x', d => cx(d.dt) - barW / 2)
       .attr('width', barW)
       .attr('y', d => d.v == null ? y(0) : Math.min(y(0), y(d.v)))
-      .attr('height', d => d.v == null ? 0 : Math.abs(y(d.v) - y(0)))
-      .attr('fill', d => (d.v ?? 0) >= 0 ? '#66bb6a' : '#ef5350');
+      .attr('height', d => d.v == null ? 0 : Math.abs(y(d.v) - y(0)));
 
     // MACD & signal lines
     const lineGen = d3line<{ dt: Date; v: Num }>()
@@ -151,7 +152,6 @@
       .attr('class', 'macd-line')
       .attr('d', lineGen as any)
       .attr('fill', 'none')
-      .attr('stroke', '#4E59D0')
       .attr('stroke-width', 1.5);
 
     g.selectAll('.signal-line')
@@ -160,7 +160,6 @@
       .attr('class', 'signal-line')
       .attr('d', lineGen as any)
       .attr('fill', 'none')
-      .attr('stroke', '#F1FEC6')
       .attr('stroke-width', 1.5);
 
     this.drawYAxes(panel, y);
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/ohlc/ohlc-chart.component.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/ohlc/ohlc-chart.component.ts
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/ohlc/ohlc-chart.component.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/ohlc/ohlc-chart.component.ts
@@ -35,10 +35,10 @@
   @Input() data!: ohlcData[];
   @Input() dateScaleX!: any;
 
-  smaLines: Array<{ period: number; color: string }> = [
-    { period: 20, color: '#ff0000' },
-    { period: 50, color: '#00ff00' },
-    { period: 150, color: '#0000ff' },
+  smaLines: Array<{ period: number }> = [
+    { period: 20 },
+    { period: 50 },
+    { period: 150 },
   ];
 
   override chartType = ChartType.OHLC;
@@ -83,6 +83,15 @@
       .range([panel.innerHeight, 0])
       .nice();
 
+    g.selectAll<SVGLineElement, number>('.price-grid')
+      .data(yScale.ticks(Math.max(2, Math.floor(panel.innerHeight / 56))))
+      .join('line')
+      .attr('class', 'price-grid')
+      .attr('x1', 0)
+      .attr('x2', panel.innerWidth)
+      .attr('y1', value => yScale(value))
+      .attr('y2', value => yScale(value));
+
     console.log('Wick data', this.data);
     console.log('📏 xScale range:', this.dateScaleX?.range?.());
     console.log('📏 xScale domain:', this.dateScaleX?.domain?.());
@@ -105,19 +114,19 @@
       .attr('x2', d => x0(d) + bw / 2)
       .attr('y1', d => yScale(d.high))
       .attr('y2', d => yScale(d.low))
-      .attr('stroke', '#B0BEC5')
-      .attr('stroke-width', 2);
+      .attr('class', 'wick');
 
     // Body
     g.selectAll('.body')
       .data(this.data)
       .join('rect')
       .attr('class', 'body')
+      .classed('body--up', d => d.close >= d.open)
+      .classed('body--down', d => d.close < d.open)
       .attr('x', d => x0(d) + (bw - candleWidth) / 2) // center in band
       .attr('y', d => yScale(Math.max(d.open, d.close)))
       .attr('width', candleWidth)
-      .attr('height', d => Math.max(1, Math.abs(yScale(d.open) - yScale(d.close))))
-      .attr('fill', d => (d.close >= d.open ? '#66bb6a' : '#ef5350'));
+      .attr('height', d => Math.max(1, Math.abs(yScale(d.open) - yScale(d.close))));
 
     console.log(`✅ OHLC drawn (${caller})`);
 
@@ -180,7 +189,7 @@
       .y(d => y(d.v as number))
       .curve(curveLinear);
 
-    this.smaLines.forEach(({ period, color }) => {
+    this.smaLines.forEach(({ period }) => {
       const series = sma(closes, period);                         // (number|null)[]
       const pathData = dates
         .map((dt, i) => ({ dt, v: series[i] as Num }))
@@ -193,7 +202,6 @@
         .attr('class', `sma-line sma-${period}`)
         .attr('d', lineGen as any)
         .attr('fill', 'none')
-        .attr('stroke', color)
         .attr('stroke-width', 1.5);
     });
   }
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/rsi/rsi-chart.component.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/rsi/rsi-chart.component.ts
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/rsi/rsi-chart.component.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/rsi/rsi-chart.component.ts
@@ -129,7 +129,6 @@
       .attr('class', 'rsi-line')
       .attr('d', lineGen as any)
       .attr('fill', 'none')
-      .attr('stroke', '#85ad90')
       .attr('stroke-width', 1.5);
 
     this.drawYAxes(panel, y);
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/volume/volume-chart.component.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/volume/volume-chart.component.ts
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/volume/volume-chart.component.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/volume/volume-chart.component.ts
@@ -4,6 +4,7 @@
 import { select } from 'd3-selection';
 import { scaleLinear, type ScaleBand } from 'd3-scale';
 import { axisLeft, axisRight } from 'd3-axis';
+import { format as d3format } from 'd3-format';
 
 import { ChartType } from '../../enums/chart-type';
 import { ohlcData } from '../../interfaces/techan-interfaces';
@@ -76,11 +77,12 @@
       .data(this.data)
       .join('rect')
       .attr('class', 'vol-bar')
+      .classed('vol-bar--up', d => d.close >= d.open)
+      .classed('vol-bar--down', d => d.close < d.open)
       .attr('x', d => this.dateScaleX(asDate(d.date)) ?? 0)
       .attr('y', d => yScale(d.volume ?? 0))
       .attr('width', barW)
-      .attr('height', d => Math.max(1, contentHeight - yScale(d.volume ?? 0)))
-      .attr('fill', d => (d.close >= d.open ? '#5AA469' : '#D46A6A'));
+      .attr('height', d => Math.max(1, contentHeight - yScale(d.volume ?? 0)));
 
     this.drawYAxes(panel, yScale);
   }
@@ -92,11 +94,14 @@
     this.innerHeight = contentHeight;
 
     const ticks = this.yTickCount(contentHeight);
+    const compactVolume = (value: number): string =>
+      value === 0 ? '0' : d3format('.2~s')(value).replace('G', 'B');
 
     select(this.gAxisLeft.nativeElement)
       .call(
         axisLeft(yScale)
           .ticks(ticks)
+          .tickFormat(value => compactVolume(value as number))
           .tickSizeOuter(0)
       );
 
@@ -104,6 +109,7 @@
       .call(
         axisRight(yScale)
           .ticks(ticks)
+          .tickFormat(value => compactVolume(value as number))
           .tickSizeOuter(0)
       );
   }
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/layout/chart-scaffold-builder.service.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/layout/chart-scaffold-builder.service.ts
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/layout/chart-scaffold-builder.service.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/layout/chart-scaffold-builder.service.ts
@@ -10,8 +10,8 @@
       height,
       margins: {
         bottom: 30,
-        left: 40,
-        right: 40,
+        left: 58,
+        right: 58,
         top: 30
       },
       xAxisTop: 30,
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss
@@ -1,9 +1,26 @@
 /*technical-analysis.component.scss*/
 
 jz-technical-analysis {
-  --axis-tick-color: #c6c5b9;
-  --axis-line-color: #c6c5b9;
-  --axis-label-color: #c6c5b9;
+  --chart-surface: var(--plt-clr-2);
+  --chart-separator: color-mix(in srgb, var(--plt-txt-2) 18%, transparent);
+  --chart-grid: color-mix(in srgb, var(--plt-txt-2) 11%, transparent);
+  --chart-axis: color-mix(in srgb, var(--plt-txt-2) 56%, transparent);
+  --chart-label: color-mix(in srgb, var(--plt-txt-1) 76%, transparent);
+
+  --chart-bullish: #68b77a;
+  --chart-bearish: #dc6b6b;
+  --chart-wick: #aab8c2;
+  --chart-overlay-fast: #e6ad51;
+  --chart-overlay-medium: #49b3a7;
+  --chart-overlay-slow: #7889d8;
+  --chart-macd: #7889d8;
+  --chart-signal: #d8dca8;
+  --chart-rsi: #8eb59a;
+  --chart-reference: color-mix(in srgb, var(--plt-txt-2) 24%, transparent);
+
+  --axis-tick-color: var(--chart-axis);
+  --axis-line-color: var(--chart-axis);
+  --axis-label-color: var(--chart-label);
 }
 
 .svg-container{
@@ -11,7 +28,7 @@
 }
 
 .rSvgElement {
-     fill:var(--plt-clr-2);
+     fill: var(--chart-surface);
      border-radius:8px;
 }
 
@@ -57,19 +74,17 @@
   font-size: 12px;
 }
 
-.tick {
-  stroke:rebeccapurple ;
-}
-                                                                                                                                                                      
 .tick line {
   stroke: var(--axis-line-color);
+  stroke-opacity: .42;
 }
 
-  .tick text {
-    fill: #c6c5b9 !important;
-    stroke: none;
-    font-size: 10px;
-  }
+.tick text {
+  fill: var(--axis-label-color) !important;
+  stroke: none;
+  font-size: 10px;
+  font-variant-numeric: tabular-nums;
+}
 
 .x-axis {
 /*  color: var(--plt-chart-2);
@@ -82,24 +97,85 @@
 }
 
   .x-axis > .domain {
-    color: red;
     stroke: var(--axis-line-color);
     stroke-width: 1;
   }
 
 .y-axis {
-  color: var(--axis-color, #98d0f6);
+  color: var(--axis-color, var(--chart-axis));
 }
 
 .y-axis .domain,
 .y-axis .tick line {
-  stroke-opacity: .35;
+  stroke: var(--axis-line-color);
+  stroke-opacity: .36;
 }
 
 .y-axis .tick text {
   font-size: 10px;
 }
 
+
+.panel-host > .panel-debug {
+  fill: transparent !important;
+  stroke: var(--chart-separator);
+  stroke-width: 1;
+  vector-effect: non-scaling-stroke;
+}
+
+.price-grid {
+  stroke: var(--chart-grid);
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+  pointer-events: none;
+}
+
+.wick {
+  stroke: var(--chart-wick);
+  stroke-width: 1.25;
+}
+
+.body--up,
+.vol-bar--up,
+.macd-hist--positive { fill: var(--chart-bullish); }
+
+.body--down,
+.vol-bar--down,
+.macd-hist--negative { fill: var(--chart-bearish); }
+
+.vol-bar { opacity: .78; }
+.macd-hist { opacity: .86; }
+
+.sma-line,
+.macd-line,
+.signal-line,
+.rsi-line {
+  fill: none;
+  stroke-linecap: round;
+  stroke-linejoin: round;
+  vector-effect: non-scaling-stroke;
+}
+
+.sma-20 { stroke: var(--chart-overlay-fast); }
+.sma-50 { stroke: var(--chart-overlay-medium); }
+.sma-150 { stroke: var(--chart-overlay-slow); }
+.macd-line { stroke: var(--chart-macd); }
+.signal-line { stroke: var(--chart-signal); }
+.rsi-line { stroke: var(--chart-rsi); }
+
+.macd-baseline,
+.rsi-guide {
+  stroke: var(--chart-reference);
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+
+.rsi-band-ob,
+.rsi-band-os {
+  fill: var(--chart-bearish);
+  opacity: .035;
+}
+
 .title-text {
   fill: lightgray;
 }

