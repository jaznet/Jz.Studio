diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.html b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.html
index 2db2e730ae0be2a58295127bed59f0f520a52c5d..fdb49de35c84549c23e0b2c35d2e0512fb6dc310 100644
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.html
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.html
@@ -13,6 +13,7 @@
     <g #gContent id="gContent">
       <rect #rContent id="rContent" width="0" height="0"></rect>
       <g #gChart id="gChart"></g>
+      <text class="panel-label" x="8" y="14">{{ panelLabel }}</text>
     </g>
 
     <g #gAxisGroupRight id="gAxisGroupRight">
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.ts
index 266cb21fd2ca9baebf78402f276406fbfd0c4d92..952c75b46d8df5d3c4e668c8fb9922d61de99897 100644
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/charts/base-chart/base-chart.component.ts
@@ -50,6 +50,12 @@ export abstract class BaseChartComponent implements OnChanges, AfterViewInit {
 
   chartType: ChartType = ChartType.Base;
 
+  get panelLabel(): string {
+    return this.chartType === ChartType.OHLC
+      ? 'PRICE'
+      : this.chartType;
+  }
+
   ngAfterViewInit(): void {
     this.viewInitialized = true;
     this.checkAndDraw('ngAfterViewInit');
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/rendering/chart-x-axis.service.ts b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/rendering/chart-x-axis.service.ts
index aae8e24cff4b0a5abd73d9ff016e60a377ad8e4a..777e25ba7103a4b419150892a14d9735019854c1 100644
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/rendering/chart-x-axis.service.ts
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/engine/rendering/chart-x-axis.service.ts
@@ -7,8 +7,6 @@ import { timeFormat } from 'd3-time-format';
 import { ChartScaffold } from '../../interfaces/chart-scaffold.interface';
 import { StockPriceHistory } from '../../models/stock-price-history.model';
 
-type AxisDomainValue = string | number | Date | { valueOf(): number };
-
 @Injectable({ providedIn: 'root' })
 export class ChartXAxisService {
   createScale(
@@ -36,44 +34,46 @@ export class ChartXAxisService {
     bottomElement: SVGGElement,
     scale: ScaleBand<Date>
   ): void {
-    const topAxis = axisTop(scale).tickFormat(this.createAdaptiveFormatter(scale));
-    const bottomAxis = axisBottom(scale).tickFormat(this.createAdaptiveFormatter(scale));
+    const dates = scale.domain();
+    if (dates.length === 0) return;
+
+    const spanDays = this.spanDays(dates);
+    const tickValues = this.selectLabelDates(dates, scale, spanDays);
+    const tickFormat = spanDays <= 120
+      ? timeFormat('%b %-d')
+      : spanDays <= 730
+        ? timeFormat('%b %Y')
+        : timeFormat('%Y');
+
+    const topAxis = axisTop(scale)
+      .tickValues(tickValues)
+      .tickFormat(tickFormat)
+      .tickSize(5)
+      .tickSizeOuter(0);
+    const bottomAxis = axisBottom(scale)
+      .tickValues(tickValues)
+      .tickFormat(tickFormat)
+      .tickSize(5)
+      .tickSizeOuter(0);
 
     select(topElement).call(topAxis);
     select(bottomElement).call(bottomAxis);
   }
 
-  private createAdaptiveFormatter(
-    scale: ScaleBand<Date>
-  ): (value: AxisDomainValue) => string {
-    const dates = scale.domain();
-    if (dates.length === 0) return () => '';
-
+  private spanDays(dates: readonly Date[]): number {
     const firstDate = dates[0];
     const lastDate = dates[dates.length - 1];
-    const spanDays = Math.max(
+    return Math.max(
       1,
       (lastDate.getTime() - firstDate.getTime()) / 86_400_000
     );
-    const labelDates = this.selectLabelDates(dates, scale, spanDays);
-    const formatLabel = spanDays <= 120
-      ? timeFormat('%b %-d')
-      : spanDays <= 730
-        ? timeFormat('%b %Y')
-        : timeFormat('%Y');
-
-    return value => {
-      const date = this.toDate(value);
-      if (!date) return '';
-      return labelDates.has(date.getTime()) ? formatLabel(date) : '';
-    };
   }
 
   private selectLabelDates(
     dates: readonly Date[],
     scale: ScaleBand<Date>,
     spanDays: number
-  ): ReadonlySet<number> {
+  ): Date[] {
     const candidates = dates.filter((date, index) => {
       if (index === 0) return true;
 
@@ -107,7 +107,7 @@ export class ChartXAxisService {
       }
     }
 
-    return new Set(selected.map(date => date.getTime()));
+    return selected;
   }
 
   private weekKey(date: Date): string {
@@ -116,12 +116,4 @@ export class ChartXAxisService {
     startOfWeek.setDate(date.getDate() - date.getDay());
     return startOfWeek.toISOString().slice(0, 10);
   }
-
-  private toDate(value: AxisDomainValue): Date | null {
-    if (value instanceof Date) return value;
-    if (typeof value === 'string' || typeof value === 'number') {
-      return new Date(value);
-    }
-    return null;
-  }
 }
diff --git a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss
index 8e6bf9996172e8e4e6449cd20dc8df675a43f6bb..32195da0e63c587b895a97fa2b857d8c282ca20c 100644
--- a/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss
+++ b/jz.studio.client/libraries/apps/jz-technical-analysis/src/lib/technical-analysis.component.scss
@@ -92,14 +92,24 @@ fill:transparent;
 }
 
 .x-axis > .tick {
-  color: var(--plt-chart-2);
+  color: var(--axis-line-color);
   stroke-width: 1;
 }
 
-  .x-axis > .domain {
-    stroke: var(--axis-line-color);
-    stroke-width: 1;
-  }
+.x-axis > .tick line {
+  stroke-opacity: .58;
+}
+
+.x-axis > .tick text {
+  font-size: 10px;
+  letter-spacing: .01em;
+}
+
+.x-axis > .domain {
+  stroke: var(--axis-line-color);
+  stroke-opacity: .72;
+  stroke-width: 1;
+}
 
 .y-axis {
   color: var(--axis-color, var(--chart-axis));
@@ -123,6 +133,15 @@ fill:transparent;
   vector-effect: non-scaling-stroke;
 }
 
+.panel-label {
+  fill: color-mix(in srgb, var(--chart-label) 62%, transparent);
+  font-size: 9px;
+  font-weight: 600;
+  letter-spacing: .12em;
+  pointer-events: none;
+  text-transform: uppercase;
+}
+
 .price-grid {
   stroke: var(--chart-grid);
   stroke-width: 1;
